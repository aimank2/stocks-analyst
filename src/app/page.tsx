import { Button } from "@/components/ui/button";

const Home = () => {
  return (
    <div className="flex flex-center h-screen">
      <Button>Click</Button>
    </div>
  );
};

export default Home;
